#ifdef MINROOT_HEAP
#define MINROOT_HEAP
#include <iostream>
#include <vector>
using namespace std;
template <typename T>
class MinHeap{
private:
    vector<T> arry;

public:
    MinHeap(){}
    MinHeap(const MinHeap & other){
        arry.resize(other.size());
        arry.
    }
};

#endif
